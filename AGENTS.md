# AGENTS.md instructions

本机 Maven 路径：D:\tools\maven\apache-maven-3.9.16\apache-maven-3.9.16\bin\mvn.cmd

不擅自把我代码里的中文注释/接口文案/字段含义改成英文。

不要把我的中文改成乱码。

解决一个问题时，不要直接改，先帮我分析哪里出错了并给出解决方案，我同意了再改。

Dto、impl、实体类要加上详细注释。

当我输入 /a，你就创建一个新的分支。当我输入 /b，你就把代码合并到主分支上传到 git，提交信息你根据代码的改动编写，然后推送到远端，并删除该分支。当我输入 /c 就是开始执行。

写文档除了代码部分，都要中文来写。
